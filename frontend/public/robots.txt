# CEB Uva Province — Network Asset Management System
#
# This is an internal operational system, not a website. It holds the
# province's transformer, switchgear and line register, and every page
# except the sign-in screen is behind authentication.
#
# Nothing here is to be crawled or indexed, by any agent, ever. This
# file is the polite request; the noindex meta tag in index.html is the
# instruction that actually binds a crawler that fetched the page
# anyway, and authentication is what genuinely protects the data.

User-agent: *
Disallow: /
